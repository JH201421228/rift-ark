# RIFT ARK — 작업 핸드아웃 (2026-08-08)

> 이 문서는 **이번 세션에서 무엇이 바뀌었고, 무엇이 왜 남았는지**의 요약이다.
> 규범은 여전히 `CLAUDE.md` 와 `docs/` 다 — 이 문서와 다르면 그쪽이 맞다.
>
> 이전 회차: [`HANDOUT-2026-08-06.md`](HANDOUT-2026-08-06.md)

**한 줄:** 게임을 만드는 일에서 **출시하는 일**로 넘어갔다. 저장소·서명키·AAB·
스토어 이미지·개인정보 방침·광고가 전부 실물이 되었고, **Play 비공개 테스트에
업로드까지 끝났다.**

| | |
|---|---|
| 검증 | lint 0 · 테스트 **1,361**(71파일) · `data:validate` 경고 0 · `check` 전항 · `check:prod` ✅ · `store:check` ko/en ✅ |
| ⚠ `economy` | **실패(11개 구간 초과) — 결정이지 회귀가 아니다** (§3) |
| ⚠ `balance:check` | 16/21 — 예전부터의 판정 대기 (`B3`·`BN3`) |
| 산출물 | `app-release.aab` / `app-release.apk` · `versionCode 4` · `CN=Rift Ark` |
| 브랜치 | `dev` 최신 · `stage`·`main` 은 2026-08-08 오전 시점 |

---

## 0. 다음에 할 일 (여기부터 읽으면 된다)

| # | 무엇 | 누가 |
|---|---|---|
| **1** | **테스터 14명 옵트인** → 여기서 **14일 시계가 시작된다** | 사용자 |
| 2 | AdMob **차단 관리 → 광고 콘텐츠 등급 G** | 사용자 ★ 테스터가 플레이하기 전에 |
| 3 | AdMob **GDPR 메시지 · 미국 주 규정 메시지 생성 → 게시** | 사용자 |
| 4 | 실기에서 광고 6항목 확인 (§4) | 사용자 |
| 5 | 14일 뒤 **프로덕션 액세스 신청** → 오픈 | 사용자 |
| 6 | 그 뒤 **iOS + Codemagic** (§6) | — |

> ⛔ **테스터에게 광고 얘기를 꺼내지 않는다.** 유도된 클릭은 무효 트래픽이고
> AdMob 계정 정지 사유다. 자연스러운 사용은 정당하다 — 문제는 오직 유도다.

---

## 1. 출시 인프라가 0% 에서 실물이 되었다

`35-remaining-work-roadmap.md` 가 "진척 0%" 로 적어 둔 항목이 전부 닫혔다.

| | 상태 |
|---|---|
| git 원격 | `github.com/JH201421228/rift-ark` (private) · `dev`/`stage`/`main` |
| 릴리스 키스토어 | `C:\keys\riftark-release.jks` · PKCS12 · 4096-bit · 만료 2053-12-24 |
| 인증서 주체 | `CN=Rift Ark, OU=Development, O=Rift Ark, L=Seoul, ST=Seoul, C=KR` |
| AAB | 나온다. 서명 검증까지 통과 |
| 개인정보 처리방침 | **https://jh201421228.github.io/riftark-privacy/** (한/영 한 페이지) |
| Play Console | 앱 생성(무료) · 앱 콘텐츠 8항목 · **비공개 테스트 업로드 완료** |
| 아이콘·스토어 이미지 | 512 · 피처 · 스크린샷 한/영 각 16장 (전량 재촬영) |

### 1.1 ★ 데몬이 죽던 원인은 Gradle 이 아니었다

`bundleRelease` 가 `Gradle build daemon disappeared unexpectedly` 로 네 번 죽었다.
메시지에 OOM 이라는 단어가 없고 데몬이 죽어 스택도 없어서 `org.gradle.jvmargs` 를
의심했는데, **1536m → 3072m 으로 올린 뒤에도 같은 자리에서 죽었다.**

원인은 `hs_err_pid*.log` 맨 위에 있었다 — `Native memory allocation (malloc) failed`.
**힙이 아니라 시스템 커밋 한도**였고, **21시간 돌던 `npm run dev` 의 Vite 프로세스
하나가 커밋 48GB** 를 잡고 있었다 (총 64.9GB 중 여유 3.6GB). 끄자 2분 14초에 통과했다.

> **그래서 `-Xmx` 를 올리는 것이 이 증상을 악화시킨다.** 힙은 JVM 이 미리 커밋하는
> 양이라, 커밋이 부족한 상태에서 키우면 네이티브 arena 몫이 줄어든다.
> **AAB 빌드 전에는 개발 서버를 끈다.** 진단 절차는 `50 §1.4`.

### 1.2 `versionCode` — CI 와 로컬은 번호 공간을 나눠 쓴다

첫 업로드에서 *"1 버전 코드는 이미 사용되었습니다"* 로 막혔다. 한 번 올린 값은
**영원히** 재사용할 수 없다.

더 큰 것이 뒤에 있었다 — **Codemagic 의 `BUILD_NUMBER` 는 첫 빌드에서 1부터 시작한다.**
손으로 1·2 를 태운 상태로 CI 를 붙이면 첫 자동 빌드가 그대로 거부된다.

```
versionCode = VERSION_CODE_BASE + BUILD_NUMBER
  CI            1000 + BUILD_NUMBER   → 1001 부터
  로컬 수동     BUILD_NUMBER           → 1000 미만
```

> ★ **CI 인지 코드가 추측하지 않는다.** `CM_BUILD_ID`·`CI` 로 분기하면 그 이름이
> 바뀌는 날 오프셋이 조용히 0 이 되고 충돌이 돌아온다. 오프셋은 `codemagic.yaml` 의
> `vars` 에 적힌 값이라 **코드 리뷰에 보인다.**

---

## 2. 광고가 실물이 되었다

| | |
|---|---|
| 앱 ID | `ca-app-pub-6178685918745796~5932212438` (매니페스트) |
| 광고 단위 ID | `ca-app-pub-6178685918745796/2344321218` (`ads.json`) |
| 설정 | `enabled: true` · **×1.5 · 하루 무제한 · 1-1 부터** · 쿨다운 30초 |

### 2.1 ★ 켜자 잠들어 있던 테스트 17개가 깨어났다

`adReward.test.js` 전체가 `AD_ENABLED` 로 `it.skip` 되어 **한 번도 실행된 적이
없었다.** 켜는 순간 하나가 실패했다 — `dailyViews: 1` 에서는 한 번 보면 남은
횟수가 0 이라 `canWatchAd` 가 언제나 `daily` 로 먼저 막아, **`cooldownMs` 가 단 한 번도
발동할 수 없는 죽은 설정**이었다.

> **스위치 하나가 검사 17개를 동시에 깨운다.** `enabled` 같은 전역 플래그를 켤 때는
> 그 플래그로 `skip` 된 것이 있는지부터 본다.

### 2.2 ★★★ 하네스가 조용히 거짓말할 뻔했다

`tools/lib/f2p-power.mjs` 의 광고 예산이 `Number(ADS.dailyViews) || 0` 이라
**무제한 설정에서 예산을 0 으로 읽고 "광고 영향 없음"으로 보고**했다.
켜 놓은 광고를 하네스가 못 본 채 `✅ 통과` 를 찍는 것이 이 저장소가 가장
두려워하는 모양이다. 고치니 **11개 구간 초과**가 드러났다.

### 2.3 동의 철회 수단을 만들었다 — 방침의 문장을 사실로

GDPR 은 동의를 **준 것만큼 쉽게 철회**할 수 있어야 한다고 요구하는데 그 수단이
하나도 없었다. `설정 > 데이터 > 광고 개인정보 설정` 을 만들었다 (UMP 가 요구할
때만 렌더 — 한국에서는 보이지 않는다).

**그 테스트가 진짜 버그를 하나 잡았다:** `preloadRewarded()` 가 `initAds()` 의
**캐시된 반환값**을 보고 있어서, EEA 사용자가 처음에 거부했다가 나중에 동의로
바꾸면 **그 세션 내내 광고가 한 번도 안 떴다.** 예외도 로그도 없다.

---

## 3. ⚠ `npm run economy` 는 실패 상태다 — 결정이지 회귀가 아니다

사용자가 **광고 무제한 · 1-1 부터**를 선택했다 (2026-08-08). 아래 실측을 읽은 뒤의
결정이다.

| 조합 | `economy` | 최대 파워 증가 |
|---|---|---|
| ×1.5 · 하루 2회 | ✅ 통과 | +19% |
| ×1.5 · 하루 3회 | ✗ 3개 초과 | +28% |
| ×1.5 · 하루 10회 | ✗ 8개 초과 | +69% |
| **×1.5 · 무제한 · 1-1** | ✗ **11개 초과** ← 지금 | — |

**무엇이 걸려 있나:** 광고를 **안 본** 플레이어의 곡선은 그대로다. 본 플레이어는
40–60 구간의 편성 퍼즐 벽을 골드로 넘을 수 있다 — 설계 결정 5 와 충돌한다.

> ⛔ **회귀로 오해해 되돌리지 않는다.** 되돌리려면 `ads.json` 의
> `dailyViews` 를 2 로, `minStage` 를 6 으로. 그 두 줄뿐이다.
>
> ★ `economy` 는 **CI 게이트에 넣지 않았다** — 넣었으면 모든 릴리스가 막힌다.
> 로컬 `npm run verify` 는 이 항목에서 빨간색이 된다.

---

## 4. 남은 확인 — 실기에서만 드러나는 것

1. **1-1** 승리 → 결과 화면에 골드 보너스 버튼 (이전엔 1-6 부터)
2. **"오늘 n회 남음" 문구가 없는가** ← 무제한이라 그리지 않는다
3. 시청 완료 → 골드 ×1.5
4. ★ **중도 이탈 시 보상 없고 버튼이 굳지 않는가** — 플러그인이 그 경우 프로미스를
   영영 돌려주지 않아 우회해 둔 자리다
5. 광고 뒤 **시스템 바가 돌아오지 않는가** (몰입 모드 복귀)
6. 연속 시청 시 **30초 쿨다운만 걸리는가** (무제한이라 상한은 안 걸린다)

> ⛔ **테스트 기기 등록 없이 실제 광고를 클릭하지 않는다.**
> AdMob → 설정 → 테스트 기기.

---

## 5. 검사기가 통과시키던 거짓 셋

이번 세션에서 **통과 중인 검사기가 거짓을 보증하고 있던 것**을 셋 찾아 고쳤다.

| 무엇 | 어떻게 조용했나 |
|---|---|
| 스토어 스크린샷에 **"광고 없음"** 이 구워진 채였다 | `store:check` 는 `store-copy.json` **한 파일만** 읽고 픽셀은 보지 않는다. 카피는 고쳐졌는데 이미지가 이틀 낡아 있었다 → **신선도 규칙**을 넣었다 (`check-production.mjs` 의 D1 과 같은 발상) |
| 1번 스크린샷의 **하늘이 비어 있었다** | 카피는 "세 개의 레인과 하늘" 인데 고정 장면 `2-4` 에 FLYING 적이 **한 마리도 없다.** `battleScore` 는 처음부터 공중 적에 가점을 줬지만 **데이터에 없는 것을 점수가 만들 수는 없다** → 장면을 `2-8` 로 옮기고, `airEnemies == 0` 이면 **저장하지 않고 던진다** |
| 개인정보 방침이 **앱보다 관대하게** 적혀 있었다 | 존재하지 않는 설정 화면 · 동의 거부 시 동작 · 미구현 iOS ATT 셋을 약속하고 있었다 → 코드와 대조해 정정하고, 없던 기능은 **만들어서** 사실로 바꿨다 |

---

## 6. ⏸ CI(Codemagic) — iOS 를 붙일 때 연결한다

**1.0 은 CI 없이 간다.** 앱이 하나이고 릴리스가 드물어서, 손으로 올리는 비용이
CI 를 세우고 유지하는 비용보다 작다. **iOS 는 다르다** — macOS 빌드는 손으로 하기
어렵고, 그때 Codemagic 이 실제로 이득이 된다.

| | |
|---|---|
| `codemagic.yaml` | **저장소에 그대로 둔다.** 작성·검증 완료, 연결만 안 했다 |
| GCP 서비스 계정 | ⏸ 만들지 않았다 — 쓰지 않을 키를 미리 만들면 유출 표면만 는다 |
| 릴리스 절차 | `50 §9.0` (로컬 수동) |

> ★ **연결할 때 가장 먼저 볼 것:** `codemagic.yaml` 첫 스텝이 검사하는 `CM_*` 네
> 이름은 **Codemagic 문서 기준이고 한 번도 실측된 적이 없다.** 첫 실행이 1분 안에
> 실패하면서 **실제 이름 목록을 출력**하도록 만들어 두었다 — 그 목록으로
> `build.gradle` 을 맞춘다. 이 검사가 없으면 **서명 없는 AAB 가
> `BUILD SUCCESSFUL` 로 끝나고** Play 업로드에서야 실패한다.

---

## 7. 되돌릴 수 없는 것 / 아직 되돌릴 수 있는 것

| 되돌릴 수 없음 | |
|---|---|
| **Play 앱이 "무료"다** | 유료 → 무료만 허용된다. 반대는 앱을 새로 만드는 것뿐 |
| **패키지명 `com.superdimension.app`** | 첫 AAB 업로드로 고정됐다 |
| **`versionCode` 1·2·3·4** | 소진됐다. 다음은 5 이상 |
| **Play App Signing 옵트인** | 해제 불가 (대신 업로드 키 분실 시 재설정 가능해졌다) |

| 되돌릴 수 있음 | 어떻게 |
|---|---|
| 광고 자체 | `ads.json:enabled = false` 한 줄 |
| 광고 무제한 | `dailyViews` 2 · `minStage` 6 |
| CI 미사용 | `codemagic.yaml` 이 그대로 있다 |

> ★★★ **키스토어와 비밀번호의 오프라인 백업 2벌은 아직 확인되지 않았다.**
> Play App Signing 덕에 업로드 키 재설정은 가능해졌지만 며칠 걸리는 지원 절차다.
> **USB 한 벌 + 비밀번호 관리자 한 벌**(키 파일 자체도 첨부)을 아직 안 하셨으면 오늘 한다.
