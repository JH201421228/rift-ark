# CLAUDE.md — AI 개발 가이드

이 파일은 Claude Code가 이 저장소에서 작업할 때 참조하는 지침입니다.

---

## 프로젝트

**RIFT ARK / 리프트 아크** (가제) — 가로형 모바일 **레인 디펜스**.
팔라독 · 카툰워즈 계보. 한국 선행 → 글로벌.

| 항목 | 값 |
|---|---|
| 코드 위치 | `FE/` |
| 스택 | React 19 · Vite 7 · Zustand 5 · Phaser 3.90 · Capacitor 7 · Tiled |
| 화면 | **가로 고정**, 디자인 해상도 **1280 × 720**, `Phaser.Scale.RESIZE` + `game/viewport.js` (**세로 720 만 고정**, 가로는 화면비만큼 열림 → 레터박스 없음) |
| 에셋 원본 | `FE/asset/` (7,845 파일) — **직접 서빙하지 않음. 아틀라스로 패킹** |
| 문서 | `docs/` — 인덱스는 `docs/README.md` |

---

## ★ 이 저장소는 2026-08-04에 크게 잘렸다

**무엇이 사라졌는지의 단일 출처: [`docs/04-plan/34-scope-cut.md`](docs/04-plan/34-scope-cut.md).**
다른 문서와 그 문서가 다르면 **그 문서가 맞다.**

> 만들려던 것은 팔라독처럼 라이트한 레인 디펜스였는데, 그 위에 2026년식 F2P
> 운영 게임이 통째로 얹혀 있었다. **가챠 · 상점 · 배틀패스 · 광고 · 출석 ·
> 일일/주간 퀘스트 · 방치 · 파견 · 던전 · 탑 · 시험 · 도감 · 계측**을 전부
> 걷어냈다. 재화는 **골드 하나**, 화면은 **여섯 개**, 성장은 **세 갈래**다.

**되살리자고 제안하기 전에 그 문서 §0 을 읽을 것.** "있으면 좋을 것 같다"는
이유로 위 목록의 항목을 다시 넣는 것은 이 프로젝트가 방금 되돌린 바로 그 실수다.

---

## 작업 전에 반드시 읽을 것

| 상황 | 문서 |
|---|---|
| **다음에 무엇을 만들지 정할 때** | **`docs/04-plan/35-remaining-work-roadmap.md`** ← 2026-08-06 현재 남은 것 전부 + 출시 로드맵 |
| 게임을 만든 130 티켓의 기록 | `docs/04-plan/33-execution-plan.md` (사실상 완료 · 역사 문서) |
| **스토어에 낼 때** | `docs/06-release/50-google-play-paid-codemagic.md` · `51-app-store-paid-codemagic.md` · `52-store-image-codex-prompts.md` |
| **무엇이 왜 없는지 확인할 때** | **`docs/04-plan/34-scope-cut.md`** |
| 게임 설계를 바꾸려 할 때 | `docs/02-design/10-GDD.md` |
| 전투 로직을 만질 때 | `docs/03-tech/22-simulation-spec.md` |
| 상태 관리를 만질 때 | `docs/03-tech/21-state-management.md` |
| 에셋을 추가할 때 | `docs/03-tech/23-asset-pipeline.md` |
| **문자열을 만지거나 번역할 때** | **`docs/03-tech/29-i18n.md`** + 스킬 `.claude/skills/i18n/SKILL.md` |
| 밸런스 수치를 만질 때 | `docs/02-design/14-economy-balance.md` + `docs/03-tech/27-testing-balance-harness.md` |
| 코드를 쓰기 전 | `docs/03-tech/28-coding-conventions.md` |
| **게임이 멈췄다 · 예외가 났다** | **`docs/03-tech/26-performance-budget.md` §10-C** ← 진단 장치와 읽는 법 |

---

## 절대 규칙 10가지

1. **`src/game/logic/` 에 `phaser`, DOM API, `Math.random()`, `Date.now()` 를 절대 넣지 않는다.**
   전투 수학은 순수 함수 · 시드 PRNG · 고정 30Hz 틱이다. 여기서 결정론 · 밸런스 자동검증 · 리플레이 · 백그라운드 복귀 안전성이 전부 나온다.

2. **`update()` 에서 조건 없이 `gameStore.set()` 을 호출하지 않는다.**
   초당 10회 넘게 변하는 값은 Zustand에 넣지 않는다. HUD 동기화는 10Hz 스로틀.

3. **모든 Phaser 씬은 `shutdown()` 과 `installViewport(this)` 를 갖는다.**
   `shutdown()` — 구독 해제 · 트윈 킬 · 타이머 제거 · 풀 해제.
   `installViewport` — 이것이 없으면 카메라가 줌 1 · 스크롤 0 이라 `DESIGN.width/2`
   가 화면 한가운데가 **아니다.** 부팅·프리로드 씬이 그래서 왼쪽으로 치우쳐 있었다.
   둘 다 `src/game/scenes/scenes.test.js` 가 전수 검사한다.

   > **⚠ Phaser 는 씬의 `shutdown()` 을 자동으로 호출하지 않는다.**
   > 자동 연결되는 훅은 `init` · `preload` · `create` · `update` 뿐이다.
   > `GameManager.wireShutdownHooks()` 가 SHUTDOWN 이벤트에 연결해 준다 —
   > **이 연결을 지우면 전 씬의 정리 코드가 조용히 죽는다.** (2026-08-02, BGM 겹침 버그)
   > 정리 코드는 `create` 전에도 호출될 수 있으므로 **절대 throw 하지 않는다** (`?.` 사용).

4. **밸런스 수치를 코드에 하드코딩하지 않는다.** 전부 `src/game/data/*.json`.

5. **에셋 경로를 코드에 하드코딩하지 않는다.** 논리 키(`unit.holy_crusader`)로만 접근.

   > **아이콘은 두 곳에서만 온다 — 이모지·유니코드 글리프(★ ⚠ ✦ ❚❚ ▲)를 쓰지 않는다.**
   > | 게임 세계관 (재화·등급·전투 개념) | **에셋 아이콘** → `<GameIcon name="currency.gold" />`, 좌표는 `src/game/data/icons.json` |
   > | 시스템 UI (일시정지·리롤·경고·닫기) | **lucide-react** → 각 파일에서 직접 import |
   >
   > 이모지는 기기마다 글리프가 다르고(일부는 두부 □), 크기·정렬 제어 수단이 없다.
   > 상세와 경계 기준: `docs/02-design/19-art-audio-direction.md` §5.2

   > **에셋 라이선스:** `FE/asset/` 전량이 자유 사용 가능하다 (2026-08-02 확인). 크레딧 표기 목록만 `docs/legal/ATTRIBUTIONS.md` 에 관리한다. **새로 도입하는 에셋(SFX 등)은 출처를 그 파일에 함께 기록한다.**

6. **확률형 요소를 만들지 않는다. 하나도.**
   보상형 광고도 예외가 아니다 — **광고를 보면 반드시 정해진 값을 받는다.**
   "광고를 보면 확률로 더 준다"를 만들면 이 게임이 구조적으로 소멸시킨
   확률 공개 의무가 그 자리에서 되살아난다.

   가챠가 사라지면서(2026-08-04) 이 게임에는 확률형이 **한 개도 없다.**
   강화 · 합성 · 장비 옵션은 물론이고 **뽑기 자체가 없다.** 한국 게임산업법의
   확률 공개 의무와 강화형 규제가 **적용 대상 없음**이 된 상태이고, 그 상태를 지킨다.

7. **`update()` 안에서 배열 생성 · 문자열 결합 · `filter`/`map` 을 하지 않는다.** GC 스터터.

8. **반복 생성되는 객체는 반드시 풀링한다.** 발사체 · 이펙트 · 데미지 텍스트 · 유닛 스프라이트.

9. **UI는 한국어 기준으로 만든다.** 한글은 라틴 문자의 약 2배 폭이다.

   > ★★★ **다만 "영어는 저절로 들어간다"는 추론은 거짓이다** (2026-08-07 실측 정정).
   > 폰트가 진짜 2:1 듀오스페이스인 것은 맞다(라틴 0.5em · 한글 1.0em). 그런데
   > **낱말 단위로는 영어가 더 길다** — 동료 이름 50종 평균 **1.30배**, 적 이름 62종
   > **1.19배**, 최악 `황금 갑충 여왕`(14) → `Royal Scarab Matriarch`(22) = **1.57배**.
   > 한국어에 맞춘 고정 폭 상자는 **영어에서 말줄임된다.** 문자열을 만질 때
   > **두 언어를 다 본다.** 근거와 위험 지점 목록: `docs/03-tech/29-i18n.md` §4.
   >
   > **모든 사용자 노출 문자열은 `{ ko, en }` 두 언어를 함께 갖는다.**
   > UI 문구는 `src/i18n/messages/<네임스페이스>.json`, 게임 데이터의 이름·설명은
   > `game/data/*.json` 의 **그 항목 안**. `npm run check:i18n` 이 강제한다.

10. **아키텍처 결정이 바뀌면 같은 PR에서 `docs/` 를 갱신한다.** 문서와 코드가 다르면 문서가 버그다.

---

## 자주 쓰는 명령

```bash
# 개발
npm run dev                 # Vite 개발 서버
npm run lint                # ESLint (경고 0 유지)
npm run test                # Vitest
npm run test:coverage       # logic/ 커버리지 85%

# 에셋
npm run assets:pack         # asset/ → public/assets/atlas/
npm run assets:audio        # mp3 → ogg/m4a
npm run assets:all

# 데이터 & 밸런스
npm run data:validate       # JSON 스키마 + 참조 정합성
npm run economy             # 무과금이 목표 파워에 도달하는가 (스테이지 1–100)
npm run balance:quick       # 50시드 (~30초)
npm run balance:check       # 300시드 + 하드 게이트 검증
npm run balance:nightmare   # 나이트메어 게이트 BN1–BN8 만 (규칙이 실제로 편성을 바꾸는가)
npm run sigils:audit        # 각인 18종이 실제로 전투 결과를 바꾸는가 (같은 시드 2회 비교)
npm run playthrough         # 신규 계정이 확정 지급만으로 100 스테이지를 완주하는가

# 구조 검사 — "만들었는데 아무도 못 쓰는 것"을 기계가 찾는다
npm run check               # screens + unlocks + a11y + i18n (1초 미만)
npm run check:screens       # 전 화면 도달 경로
npm run check:unlocks       # 해금 전수 (단조성 · 도달성 · 선행 정합 · 라우터)
npm run check:a11y          # 접근성 배선
npm run check:i18n          # 두 언어가 다 있는가 · 하드코딩 한국어가 남았는가
npm run check:prod          # vite build + dist 안의 디버그 잔재 (약 20초)

# 아이콘 · 스플래시 (게임 스프라이트에서 합성한다 — 저장소에 원본을 던져 넣지 않는다)
npm run icons               # android/res 전 밀도 + resources/ 스토어 원본
npm run icons:check         # 존재·크기 검사 (verify 에 포함)

# 빌드
npm run build
npm run build:android
npm run build:ios
```

> `npm run verify` 가 위를 전부 순서대로 돈다:
> `lint → test → data:validate → check → icons:check → economy → balance:check → playthrough → check:prod`.

---

## 현재 상태 (2026-08-07)

**빌드된다. 게임이 돈다. 그리고 라이트해졌다.**

| | |
|---|---|
| 캠페인 | **100 스테이지** (월드 1–5) · 적 62종 · 보스 10체 · **동료 50종** · 각인 18 |
| 동료 획득 | 스테이지 **확정 지급 10종** + **골드 영입 40종** (`logic/recruit.js` · 확률 없음) |
| 전투 | 레인 3 + 공중 · 오라 · 각인 드래프트(18종 · HUD 표시) · 지휘관 주문 **12종 중 4종 장착**(연출·효과음 있음) · **지휘관 평타**(사거리 띠 표시) · 크리티컬 · 모드 2종(격퇴 · 보스) · 보스 3페이즈 |
| 성장 | **골드 하나**로 동료 레벨 · 방주 시설 4종 · **지휘관 레벨**. 별은 별 트리로 |
| 지휘관 | **레벨 30**(골드) + **장구 3슬롯 × 4점**(스테이지 확정 지급) + **성소**(오라 반경) — `docs/02-design/21-commander-growth.md` |
| 화면 | **타이틀** · 방주(홈 대시보드) · 출격 · 편성(+지휘관) · 동료 · 설정 · 전투 — 라우트 **7개**, 탭 5칸 |
| 난이도 | **노멀 · 하드 · 나이트메어** — 나이트메어는 배율 위에 **월드별 규칙 1종**(역병 장판 · 결박 파열 · 고갈). `docs/02-design/22-nightmare.md` |
| 언어 | **한국어 · 영어** — 전 화면 머리글에 토글, 설정에 정식 행. UI 628키 + 게임 데이터 전량이 `{ko,en}`. `npm run check:i18n` 이 강제한다 (`docs/03-tech/29-i18n.md`) |
| 아이콘 | 게임 스프라이트에서 **합성**한다 (`npm run icons`) — 전 밀도 + 적응형 + 스플래시. Capacitor 기본값은 2026-08-07 에 사라졌다 |
| 온보딩 | **가이드 오버레이 23주제** (`[?]` · 모달 · 수치는 데이터에서) — 단계별 튜토리얼은 **삭제**됐다 |
| 세이브 | `SAVE_VERSION` **18** · **슬롯 3** (`store/slots.js` — 슬롯 1 은 옛 키 그대로) · `meta.runSeq` 가 전투 시드의 출처 |
| 밸런스 | `balance:check` **16/21** (300시드 · 2026-08-05 재검증) — 하드 실패 2: **B3**(1-9 84.3% / 목표 30–45%) · **BN3**(5-20 나이트메어 0% — 규칙을 꺼도 0%). 소프트 실패 3: B10 · BN5 · BN8. 전부 **사용자 결정 대기**이지 회귀가 아니다 |
| 검증 | lint 0 · 테스트 **1,361**(71파일) · `data:validate` **경고 0** · `check`(screens·unlocks·a11y·**i18n**) **통과** · `icons:check` **통과** · `check:prod` **통과**(A1–A4 광고 배선 포함) · `economy` **통과**(광고 켠 상태) |

> **타이틀(`/`)이 시작점이다.** 슬롯을 고르기 전에는 게임 안이 아니라서
> `/` 에서는 탭 바가 뜨지 않고, 도달 검사기도 `/` 에는 유입 링크를 요구하지 않는다.
>
> ⛔ **단계별 튜토리얼(FTUE)은 없다 (2026-08-04 삭제).** `ftue.json` · `logic/ftue.js` ·
> `ftueSlice` · `FtueOverlay` · 전 화면의 `data-ftue` 앵커가 전부 사라졌다.
> 되살리자고 제안하기 전에 `docs/04-plan/34-scope-cut.md` ⑨ 를 읽을 것 —
> 두 번의 전수 주행에서 무엇이 계속 깨졌는지가 적혀 있다.
>
> **설명은 가이드가 한다.** 각 화면 헤더의 `[?]` → 23주제.
> 본문에 **수치를 적지 않는다** — `logic/guide.js:guideFacts()` 가 데이터에서 읽어
> 표로 만들고, `data:validate` 가 그 규약을 강제한다.
>
> ★ **되돌릴 수 없는 선택 앞에서는 확인 모달이 선다** (2026-08-05).
> 각인 선택 · 리롤 · 동료 레벨업 · 방주 시설 강화 · 지휘관 레벨업 —
> 전부 `components/ConfirmModal.jsx` 하나를 쓴다. 모달은 "정말요?"만 묻지 않고
> **치를 값과 도달할 상태를 숫자로** 말한다. `+10` 이 상한·잔액에 걸리면
> "10번 중 3번만 오릅니다"까지 말한다 (`progression.js:levelUpPlan`).
>
> ★ **떼 유닛(`units.json:squad`)은 전부 아니면 0 이다** (2026-08-05).
> 예전에는 호출부가 `squad` 만큼 `trySummon` 을 반복해서, 마나가 마릿수만큼
> 나가고 **코스트 상승도 마릿수만큼** 일어났다 — 그래서 꼬꼬댁 닭이
> 3 → 2 → 1 마리로 줄어들었다. 지금은 `trySummon` 이 `squad` 를 알고
> 총액을 한 번에 받고 코스트 상승을 **한 단만** 기록한다.
>
> **탭은 언제나 다섯이다.** 진행도로 잠그지 않는다.
>
> ★ **시스템 바를 숨긴다 — 몰입 모드** (2026-08-06, 사용자 제보). 가로 모드의
> 3버튼 네비게이션 바는 화면 **좌우**를 먹는다 — 정확히 뒤로가기 · 탭 바 · 일시정지를
> 두는 자리다. edge-to-edge 라 WebView 는 그 아래까지 그려지지만 터치를 시스템이
> 먼저 가져가서, **버튼이 보이는데 눌리지 않았다.** 가로 고정과 같은 2중 방어다 —
> 권위는 `MainActivity.java`(sticky · 포커스 복귀마다 재적용), 런타임은
> `native/bootstrap.js:hideSystemBars()`. `BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`
> 를 쓰는 이유는 기본값에서는 가장자리를 한 번 쓸면 바가 **영구히** 돌아오기 때문이다.
> 상세: `docs/03-tech/25-capacitor-mobile.md` §3.3
>
> ★★★ **작동하는데 작동하지 않는 것처럼 보이는 것** (2026-08-07, 사용자 제보 —
> "지휘관 스킬이 제대로 작동되는지 의심스럽다"). 전수조사 결과 **시뮬은 전부 정상**
> 이었다 — 평타의 레인 판정도(이벤트 100%가 지휘관 레인), 주문 12종 전부의 효과도
> 실측으로 확인됐다. 의심이 생긴 원인은 **렌더·오디오 배선의 침묵**이었고, 다섯이었다:
>
> | 무엇 | 증상 |
> |---|---|
> | `EV.SPELL_CAST` 소비처 **0개** | 주문이 **성공하면 무음·무연출**, **실패해야만** 화면이 흔들렸다. 반응이 실패에만 있는 UI 는 "이 버튼은 작동하지 않는다"로 읽힌다 |
> | `EV.COMMANDER_ATTACK` 이 `BattleSfx` 에 없음 | 전투당 57번 휘두르는 동안 소리가 한 번도 없었다 (다른 이벤트를 쓴다는 이유만으로) |
> | 화면이 그리는 유일한 원이 **오라(192)** | 평타 사거리는 140 이고 오라는 원이라 이웃 레인을 ±166px 덮는다. 전투 시간의 **35~54%** 동안 "금색 원 안의 적을 절대 안 때리는" 그림이 있었다 → 사거리는 이제 **그 레인의 띠**로 그린다 |
> | `EV.DAMAGE` 의 데미지 타입이 **맞은 쪽** | 지휘관의 물리 평타가 술식 적 위에서 파란 숫자로 떴다. 색약을 위해 만든 장치가 정반대로 작동했다 → 이벤트가 **때린 쪽** 타입을 싣는다 |
> | 주문의 `EV.DAMAGE` 가 `(x, y)` 를 레인·상성 자리에 보냄 | 엔티티에 `y` 가 없어 상성이 언제나 0 → **주문의 "약점!/저항!" 이 한 번도 뜨지 않았다** |
>
> **기계가 잡는다:** `logic/eventWiring.test.js` 가 `EV.*` 의 emit ↔ 소비를 대조하고,
> `logic/spellEvents.test.js` 가 페이로드 규약(레인·상성·dmgType)을 검사한다.
> `commanderTarget.test.js` 의 `downUntil` 검사기는 **`logic/` 한 폴더만** 훑고 있어
> `CommanderPresenter` 의 사본을 못 잡았다 — 이제 `presenters/`·`scenes/`·`fx/` 까지 본다.
> 주문 연출은 데이터다 (`presenters.json:commander.spells` — 코드에 이펙트를 박지 않는다).
>
> ★ **지휘관은 평타를 친다 (2026-08-04).** GDD 는 지휘관을 "직접 걸어 나가 싸울 수
> 있는 유일한 존재"라고 써 놓고 공격 수단을 주지 않았다. 다만 이것은 **딜 수단이
> 아니라 미끼**다 — 사거리(140)가 **오라 반경(192)보다 짧아서**, 평타를 넣으려면
> 오라 앞쪽을 적에게 내주는 자리까지 나가야 하고 그러면 SUPPORT 가 끊기고 지휘관이
> 맞는다. `data:validate` 가 이 부등식을 강제한다.
> 설계·제약·하지 않기로 한 것: `docs/02-design/20-commander-combat.md`.
>
> ★★★ **그리고 이제 실제로 맞는다 (2026-08-05).** 위 문단의 마지막 줄("지휘관이
> 맞는다")은 4개월 동안 **거짓말**이었다 — 지휘관은 `lanes[].allies` 에 없어
> `nearestTarget` 의 후보가 아니었고, 지휘관 HP 를 깎는 코드는 **보스 슬램뿐**이었다.
> 규칙은 하나다: **적은 지휘관을 맨 마지막에 본다** — 사거리 안에 동료가 하나라도
> 있으면 그를 때리고, 하나도 없을 때에만 같은 지상 레인의 지휘관을 노린다.
> "가장 가까운 것"으로 두면 지휘관이 전선에 서는 순간 무리 전체가 몰려 **미끼가
> 아니라 즉사**가 된다. 피해에는 **상한**이 있다 (`combat.enemyHitCommanderHpRatio`
> 0.08 · 최소 12방) — 지휘관 HP 는 600 고정인데 적 ATK 는 1-1 의 10 에서 5-20 의
> 342,133 까지 가므로 배율로는 절대 맞출 수 없다. 상한 적용·차감·기절 신호는
> `logic/commanderHit.js:damageCommander` 하나가 맡고 **보스 슬램도 같은 함수를 쓴다.**
> 상세: `docs/02-design/20-commander-combat.md` §2.3.
>
> > ★ **HUD 에 지휘관 체력이 선다** (2026-08-05). 실측으로 2-5 에서 평균 600 중 546 이
> > 사라지는데 화면에 읽을 숫자가 없었다 — 대가가 실재하는데 안 보이면 플레이어는
> > 그것을 버그로 읽는다. 방주 바 옆 금색 바 · 기절 중에는 "기절". **재출격 카운트다운은
> > 넣지 않는다** (`CommanderPresenter` 가 지휘관 위에 이미 그린다). 가이드도 한 주제
> > 늘었다 (`battle.commander`).
> >
> > ★★ **"지휘관이 서 있는가"는 `commanderHit.js:commanderUp` 하나다.** 이 술어는
> > 두 항(`hp > 0` · `t >= downUntil`)인데 저장소 **일곱 곳**이 각자 적고 있었고
> > **넷이 시간 항만** 보고 있었다. 그것들이 맞았던 것은 `stepCommander` 가 앞에서
> > 도는 **순서 덕분**이지 조건이 옳아서가 아니다 — 지휘관이 죽는 그 틱에는
> > `downUntil` 이 아직 0 이라 시간만 보면 **HP 0 인 지휘관이 살아 있는 것으로
> > 읽힌다.** 실제로 그 틱에는 죽은 지휘관이 이동했고 주문이 나갔다.
> > `commanderTarget.test.js` 가 `downUntil` 직접 참조를 상태를 **쓰는** 세 파일
> > (`lifecycle` · `state` · `commanderHit`)로 제한한다.
>
> ★★ **원거리 적은 발사체를 쏜다 (2026-08-05).** 그 전에는 사거리 120–190 짜리 적
> 11종이 **화면을 가로질러 즉발로** 때렸다. 규칙(`logic/roles.js:PROJECTILE_ROLES`)은
> 처음부터 양 진영을 보고 있었고, 빈 것은 **데이터**였다 — `enemies.json` 에 `role` 이
> 한 종도 없어 `normalizeDef` 의 `raw.role ?? "MELEE"` 가 62/62 를 근접으로 만들었다.
> 검사기가 그 4개월을 놓친 이유는 발사체 절이 **아군만** 돌고 있었기 때문이다.
> 지금은 `data:validate` 가 사거리 100 을 경계로 양방향 대조한다 (근접 28–72 · 원거리
> 120–190 · 간극 48px). **역할을 사거리에서 파생시키지 않는다** — 파생 규칙을 두면
> 사거리를 68→72 로 튜닝한 순간 그 적이 조용히 발사체를 쏜다.
> **아군 비행 4종도 같은 날 오후에 들어갔다** — 적을 고치고도 사거리 160–200 짜리
> FLYER 가 즉발로 남아 있었고, 사거리 대조가 **적만** 돌고 있어 또 조용했다.
> 지금은 아군도 같은 선을 쓴다. **SUPPORT 만 면제**다 (`trySupport` 는 `tryAttack` 을
> 지나지 않는다 — 힐이라 "즉발이 부당한가"가 성립하지 않는다).
> 상세: `docs/03-tech/24-data-schema.md` §2.2.
>
> ★★ **블록은 스티키다 (2026-08-04).** 한 번 붙든 적은 블로커가 죽거나 · 사거리를
> 벗어나거나 · 용량이 줄 때만 풀린다. 예전에는 매 틱 관계를 지우고 가까운 순으로
> 다시 배정해서, 용량을 넘긴 무리가 **전부 절반 속도로 계속 전진**했고 결국
> 방벽을 통과했다 ("간헐적으로 넘어간다"의 정체). `logic/blocking.test.js` 가 지킨다.
>
> ★ **모든 스테이지는 질 수 있어야 한다.** 방주 HP 는 월드 상수인데 스폰량은 램프라,
> 1-1~1-3 은 **스폰 전량이 자폭해도 이기는** 상태였다 (1-1: 44 피해 vs 방주 100).
> 비트별 `arkHp` override 로 30/36/60 으로 낮췄고, `data:validate` 가
> "패배가 도달 가능한가"를 100 스테이지 전수 검사한다.
>
> ★ **신규 계정의 시작 동료 2종은 `unlocks.json:startingUnits` 에서 온다.**
> 한때 `ftue.json` 의 단계별 `unlocks` 에서 파생됐는데, 그 출처가 사라지면
> **신규 계정의 보유 동료가 0종**이 되고 검사기 셋이 거짓 위에서 통과한다.
>
> ★★ **슬롯을 갈아탈 때는 저장소를 먼저 끊는다** (`store/slots.js:openSlot`).
> zustand persist 는 `setState` 를 즉시 디스크로 흘리므로, 키를 바꾼 뒤에
> 바닥을 지우면 **고른 슬롯의 세이브가 그 자리에서 파괴된다** (실측: 42 → 0).

> ★★ **나이트메어는 규칙이 있는 난이도다 (2026-08-05).** 월드마다 정확히 하나 —
> ① 역병 장판(1·3) ② 결박 파열(2·4) ③ 고갈(5). 확률은 하나도 없고 동시 엔티티도
> 늘지 않는다. 판정은 `logic/nightmare.js:nightmareFor(world)` **하나**이고
> 화면 · 프리뷰 · 시뮬 · 검사기가 같이 부른다 — 화면이 `world === 5` 로 분기하지 않는다.
> 게이트는 `npm run balance:nightmare` (BN1–BN8).
>
> ⚠ **게이트 두 항이 실패 상태이고 그것이 정직한 상태다.** 월드 5 후반은 나이트메어로
> 통과 불가능하고(규칙을 꺼도 0%), 월드 1–3 은 만렙 계정에게 규칙과 무관하게 통과된다.
> 둘 다 나이트메어가 만든 문제가 아니라 **배율 × 성장 곡선**이 드러난 것이다 —
> 되살리거나 배율을 만지기 전에 `docs/02-design/22-nightmare.md` §0-A.1 을 읽을 것.

### 남은 것 (`docs/04-plan/35-remaining-work-roadmap.md`)

- **P7-03** 1-9 패배의 質 — 구조적으로 규명됨 · **사용자가 직접 져 본 뒤** 설계 결정
- **P9-01~03 실기기 검증** — 콜드 스타트 · 세이브 유지 · 씬 전환 스트레스
- **나이트메어 배율 결정** — `22-nightmare.md` §0-A.1 의 두 항목
- ★ **출시 인프라 전량 (진척 0%)** — 이 폴더는 **git 저장소가 아니고**, 릴리스 서명 키가
  없고, AAB 를 만든 적이 없고, 스토어 계정이 없고, 아이콘이 Capacitor 기본값이다.
  **개인 계정 클로즈드 테스트 14일**이 일정의 바닥이므로 가장 먼저 시작한다.

### 진행 게이트

| 무엇 | 규칙 모듈 | 왜 |
|---|---|---|
| 캠페인 스테이지 | `logic/stageUnlock.js:canEnterStage` | `recordStageClear` 가 `highestStage = max(prev, globalIndex)` 라, 후반 스테이지를 딥링크로 한 번 이기면 방주·하드가 한꺼번에 열렸다 |

> 술어는 **화면·스토어가 같은 함수를 부른다.** 화면에만 자물쇠를 그리면
> 딥링크·다음 호출부가 그대로 통과한다.

---

## 아키텍처 요약

```
React (UI · HUD · 화면)
      ↕  Zustand 단일 스토어 (10Hz 이하 동기화)
Phaser (렌더러 — 게임 규칙 없음)
      ↓  읽기 전용
src/game/logic/ (순수 시뮬레이션 · 결정론 · Phaser 무의존)
```

| 담당 | 대상 |
|---|---|
| **React DOM** | 타이틀 · 방주 · 편성 · 동료 · 설정 · 결과 · 전투 HUD |
| **Phaser** | 데미지 숫자 · HP 바 · 히트 플래시 · 오라 링 · 모든 월드 앵커 요소 |
| **logic/** | 전투 수학 전체 |

---

## 이 게임을 정의하는 설계 결정 5가지

작업 중 이 결정들과 충돌하는 제안을 하기 전에 `docs/01-research/01-genre-analysis.md` §6 을 확인할 것.

1. **편성은 50 로스터 중 6종만.** 배제가 곧 결정이다. (팔라독은 9종 전부 쓸 수 있어서 빌드가 없었다)
2. **상성은 데미지타입 × 방어타입.** 물리는 DEF, 술식은 DEF 무시·RES만, 신성은 CORRUPT 특효. 태그가 조합되므로 단일 카운터 유닛이 지배할 수 없다.
3. **지휘관 오라가 실력 천장.** 역할마다 오라 의존 방식이 다르고, **지원 유닛만 오라 *밖*에서 작동**한다 — 이것이 "앞에 세워두면 끝"을 없앤다.
4. **웨이브마다 각인 3지선다.** 메타 성장은 각인 *선택지 수*를 늘릴 뿐 각인 수치를 올리지 않는다 (로그라이트 vs 성장 충돌 회피).
5. **벽은 항상 편성 퍼즐이고 절대 경제 벽이 아니다.** 현재 파워 + 올바른 편성 = 클리어 가능이 시뮬레이터로 검증된다 (게이트 B4).

---

## 아트 제약 (매우 중요)

**유닛 스프라이트에 공격 · 이동 · 사망 애니메이션이 없다.**
몬스터 150종은 전부 **16×16 / 4프레임 아이들 루프**뿐이고, NPC 49종도 아이들만 있다.

→ 전투 연출은 **트윈(돌진 · 리코일 · 스쿼시) + 틴트 플래시 + 이펙트 오버레이 + 히트스톱** 조합으로 만든다.
→ 연출은 `presenters.json` 의 데이터 프로파일이다. 코드에 박지 않는다.
→ 상세: `docs/02-design/19-art-audio-direction.md` §2

**픽셀 밀도 위계:** 소형(16×16)·대형(32×32) 유닛 모두 **×2 배율**이라 픽셀 밀도가 동일하다. 지휘관과 대형 보스만 ×1이며, 이것은 *의도된* 스케일 위계다 (굵은 픽셀 = 병력, 고운 픽셀 = 이름 있는 존재).

★★★ **피아는 스프라이트가 말하지 않는다 — 발밑 표식이 말한다** (2026-08-05, 사용자 제보).
양 진영이 **같은 시트**에서 오고, 바라보는 방향(`flipX`)은 16×16 대칭 그림이 많아
믿을 수 없다. 그리고 아군 체력바는 *피해를 입은 뒤에만* 뜨므로(막대밭 방지)
**만피 아군에게는 식별 단서가 하나도 없었다.**
→ **아군은 채운 하늘색 타원 · 적은 빈 붉은 링**을 발밑에 상시 깐다
   (`presenters.json:sideMark` · `EnemyBadges.drawSideMark`).
→ **색만으로 구분하지 않는다** — 모양이 1차 단서다. 색약 모드에서도,
   방주 피격으로 화면 전체가 붉게 물드는 순간에도 갈린다.
→ 표식은 만피 아군 `continue` **위**에 있어야 한다. 아래로 내리면 정확히
   고치려던 그 경우에만 안 그려진다 (`EnemyBadges.test.js` 가 지킨다).

---

## 커밋

```
<타입>(<범위>): <요약>

<왜 이렇게 했는지>

<밸런스 변경 시>
balance: B2 89.2% → 91.4%, B4 all pass, B7 44/44
```

타입: `feat` `fix` `perf` `refactor` `balance` `docs` `test` `chore` `art` `data`
범위: `sim` `scene` `store` `ui` `hud` `pipeline` `native` `data`

---

> **JSX 컴포넌트 import 누락은 lint 가 못 잡는다.** ESLint 의 `no-undef` 는 JSX 요소
> 이름을 보지 않고 이 저장소에는 `eslint-plugin-react` 가 없다. `SettingsScreen` 이
> 그렇게 통째로 죽어 있었다 — `src/renderSafety.test.js` 가 이제 전 `.jsx` 를 검사한다.

> **같은 사실을 두 곳에 적으면 반드시 갈라진다 — 이 저장소의 단일 실패 유형이다.**
> 2026-08-05 하루에만 세 건이 같은 모양으로 나왔다:
> ① `units.json` 에 `projectile` 과 `art.projectile` 이 **둘 다** 있었고 씬·검사기·
> 정규화가 서로 다른 쪽을 읽어, **검사기가 통과시킨 그림과 화면에 뜬 그림이 달랐다**
> (30종이 전부 같은 주황 구체를 쏘고 있었다).
> ② 편성 화면이 대공 여부를 `tags.includes("ANTI_AIR")` 로 **직접** 판정해,
> 술식·신성 동료는 공중에 닿는데도 표시가 없었다 — 판정은 `combat.js:canHitFlying`
> 하나여야 한다 (`logic/roles.js:unitHitsAir` 경유).
> ③ 씬이 데미지 타입 틴트 표를 `fx.json` 에서 **복사해** 코드에 박아 두었다.
>
> **새 값을 읽어야 하면 규칙 모듈을 경유한다. 화면이 스스로 판정하지 않는다.**

> ★★★ **위치로 실어 보낸 목록은 어긋나도 아무도 실패하지 않는다** (2026-08-06, 제보 ①).
> 씬이 주문 쿨다운을 `equipped.map(...)` **배열**로 넘기고 HUD 가 `cooldowns[i]` 로
> 읽고 있었다. 두 쪽이 같은 목록을 본다는 **가정**이 유일한 보증이었는데, 그 가정은
> 깨져 있었다 — `BattleScreen` 이 씬 페이로드에 **`spells` 를 빠뜨려서** 시뮬은
> 4개월 내내 `spells.json:defaultLoadout` 4종으로 싸웠고 HUD 는 플레이어가 고른 4종을
> 그렸다. 그래서 **쿨다운이 쓰지도 않은 주문 위에 떴고**, 기본 장착 밖의 주문은
> 눌러도 `unequipped` 로 조용히 튕겼다. 지금은 **`{주문id: 비율}`** 로 실어 보낸다 —
> 어긋나면 그냥 없는 키(0)가 되어 오배치가 **구조적으로 불가능**하다.
> `hud/spellCooldown.test.js` 가 둘 다 지킨다 (id 결합 · 페이로드 키 대조).
>
> > **씬 페이로드는 `buildStageConfig` 가 읽는 것을 전부 담아야 한다.** 화면이 만든
> > `cfg` 는 표시용이고 **씬은 같은 인자로 그 함수를 다시 부른다** — 여기서 빠진 값은
> > 전투에 존재하지 않는다. 지휘관 HP 게이지(08-05)와 주문 4칸(08-06)이 같은 사고다.
>
> ★★ **참/거짓으로 물으면 셋인 것이 거짓말을 한다** (2026-08-06, 제보 ③).
> 각인 드래프트가 `isRetroactive()` 하나로 "다음 소환부터 / 이미 나와 있는 동료에게도"를
> 갈랐는데, 그 술어는 `modifyStat` 훅만 본다. **`modifyStat` 훅이 아예 없는 각인 8종**
> (오라 · 방주 · 발사체 · 타격/처치 훅)이 전부 거짓으로 떨어져 **"다음에 소환하는
> 동료부터"라는 거짓 문구**를 달고 있었다 — 오라 반경이나 방주 최대 HP 에는 '동료
> 소환'이라는 개념 자체가 없다. 지금은 `logic/sigils.js:sigilTiming()` 이
> `immediate | retroactive | nextSummon` 셋을 답하고 화면은 문구만 고른다.
> **참/거짓 별칭은 남기지 않았다** — 남기면 다시 둘로 묻는 호출부가 생긴다.
>
> ★★ **전투 시드는 화면의 지역 상태에서 오면 안 된다** (2026-08-06, 제보 ②).
> `seed: 1000 + runKey * 7919` 의 `runKey` 가 `useState(0)` 이라, 화면을 나갔다
> 들어오면 0 으로 돌아가 **모든 첫 출격이 시드 1000** 이었다. 각인 3지선다는
> `rng.sigil` 스트림에서 나오므로 **매판 같은 조합**이 떴다. 바뀌는 것은 *같은 화면
> 안에서의 재도전*뿐이었는데 주석은 "재도전마다 바뀐다"고만 적어 두어 아무도
> 의심하지 않았다. 지금은 **저장되는 단조 카운터** `meta.runSeq` 에서 온다
> (`nextRunSeed()`) — `Math.random()`·`Date.now()` 를 쓰지 않는다. **시드는
> 재현 가능해야 제보 한 판을 그대로 다시 돌릴 수 있다.**
>
> **선언했는데 아무도 읽지 않는 것 — 이 저장소의 또 다른 단일 실패 유형이다.**
> `enemies.json:archetype`(62/62 · 참조 0) · `worlds.json:bossGiant`(2건 · 생성기가
> 읽지 않았고 값도 실제의 1/3) · `fx.json` 이펙트 4종 · `EVT.REQUEST_COMMANDER_MOVE`
> (emit 0 · on 0) 가 전부 같은 모양이었다. 문법은 완전하고 아무도 실패하지 않으며,
> 기획자는 그 값을 고치며 튜닝하고 화면은 아무 반응도 하지 않는다.
> 2026-08-05 에 전부 정리했고, 이제 **기계가 잡는다**:
> `tools/validate-data.mjs` 의 **선언 ↔ 소비 대조** 절(데이터 필드 · fx 이펙트 ·
> 아이콘 키 · EVT)과 `src/game/wiring.test.js`(W1–W5).
> **데이터에 새 필드를 더하려면 읽는 코드를 `FIELD_CONSUMERS` 에 함께 등록한다** —
> 등록 없는 필드는 `data:validate` 가 오류로 잡는다.

> **`display:flex` 인 클래스에 문장을 넣지 않는다.** 플렉스 컨테이너는 자식 텍스트
> 런 하나하나를 익명 아이템으로 만들기 때문에, 문장 중간에 `<b>` 가 하나 들어가면
> 문장이 **세 토막으로 갈라져 옆으로 눕는다** (2026-08-04 제보 — 동료 > 성장 탭의
> 무기고 안내문). 줄바꿈이 필요한 안내문에는 `display:block` 인 클래스(`.prose`)를
> 함께 준다 — `src/textLayout.test.js` 가 CSS 의 `display` 를 읽어 전 `.jsx` 를 대조한다.

> **HTML 속성이 대신 막아 줄 것이라고 믿지 않는다 — 두 번 당했다.**
> | `hidden` | UA 규칙 `[hidden]{display:none}` 은 **작성자 스타일시트보다 약하다.** `.body{display:flex}` 가 이겨서 지휘관 탭에 편성 화면이 통째로 겹쳐 그려졌다 (2026-08-05). → **조건부 렌더**를 쓴다 |
> | `disabled` | 브라우저가 막는 것은 `click` · `mousedown` 뿐이고 **`pointerdown`/`pointerup` 은 그대로 간다.** 홀드 툴팁 때문에 탭을 포인터 이벤트로 판정하는 카드에서는 아무것도 막히지 않아, **이미 보유한 동료를 눌러도 "영입하시겠습니까 · 2,500골드" 모달이 떴다** (확인해도 조용히 아무 일 없음, 2026-08-05). → **콜백 첫 줄에서 규칙 모듈로 다시 거른다** (`recruitCard.test.js`) |
>
> 둘 다 **화면을 진짜 입력으로 눌러 보기 전까지** lint · 테스트 · 검사기가 전부 통과했다.
> 합성 이벤트(`dispatchEvent`)로는 재현되지 않는다 — CDP `Input.dispatchMouseEvent` 를 써야 보인다.

---

## 리뷰 반려 사유 (자동)

1. `src/game/logic/` 에서 `Math.random()` 또는 Phaser import
2. `update()` 에서 조건 없는 `gameStore.set()`
3. 씬에 `shutdown()` 없음 또는 구독 해제 누락
4. 밸런스 수치 하드코딩
5. 에셋 경로 하드코딩 — 아이콘을 이모지/글리프로 넣는 것 포함
6. `update()` 안에서 배열 생성 / 문자열 결합
7. 새 반복 객체에 풀링 없음
8. `useGameStore(s => s)`
9. **확률형 요소 추가 (어디든)**
10. 세이브 스키마 변경에 `migrate` 누락

---

## 하지 말 것

- **`docs/04-plan/34-scope-cut.md` §2 목록을 되살리는 것** — 사용자가 직접 요청하기 전까지
- 실시간 동기 PvP / 실시간 협동 레이드 / 실시간 길드전 — **영원히 만들지 않는다**
- 스테미나 시스템
- 편성 슬롯 판매
- 전면 광고 · 강제 광고
- **전면 광고 · 배너 광고** — 보상형 하나만 쓴다

  > ★★★ **보상형 광고는 붙는다 — 무료 앱 · 클리어 골드 ×1.5 · 하루 무제한 · 1-1 부터**
  > (결정 2026-08-07 · 수치 확정 2026-08-08 · **1.0 부터 켠 채로 낸다**).
  >
  > 같은 날 계산으로는 유료가 유리했다 (설치당 $3.39 vs $0.14~0.26 ·
  > `docs/06-release/55-monetization-decision.md`). 사용자는 그 계산을 읽은 뒤에도
  > **유료의 진입장벽이 설치를 더 줄인다**고 판단해 무료+광고로 결정했다.
  >
  > ## ⚠ `npm run economy` 는 **실패 상태이고 그것이 정직한 상태다**
  >
  > | 조합 | `economy` | 최대 파워 증가 |
  > |---|---|---|
  > | ×1.5 · 하루 2회 | ✅ 통과 | +19% |
  > | ×1.5 · 하루 3회 | ✗ 3개 구간 초과 | +28% |
  > | ×1.5 · 하루 10회 | ✗ 8개 구간 초과 | +69% |
  > | **×1.5 · 무제한 · 1-1 부터** | ✗ **11개 구간 초과** ← 지금 값 | — |
  >
  > **사용자가 위 실측을 읽고 수익을 택했다** (2026-08-08). B3·BN3 와 같은 성격의
  > **결정 대기가 아니라 결정 완료**다 — 회귀로 오해해 되돌리지 않는다.
  > 되돌리는 방법은 `ads.json` 의 `dailyViews` 를 2 로, `minStage` 를 6 으로
  > 되돌리는 것 하나뿐이다.
  >
  > 무엇이 걸려 있는지: 광고를 **안 본** 플레이어의 곡선은 그대로다. 본 플레이어는
  > 40–60 구간의 편성 퍼즐 벽을 골드로 넘을 수 있다 — 설계 결정 5 와 충돌한다.
  > 스토어 문구("광고를 한 번도 보지 않아도 100 스테이지를 전부 클리어할 수
  > 있습니다")는 **여전히 참**이지만, 안 본 사람이 확연히 불리해진다.
  >
  > ★ **`economy` 를 CI 게이트에 넣지 않았다** (`codemagic.yaml`) — 넣었으면 모든
  > 릴리스가 막힌다. `npm run verify` 는 로컬에서 이 항목에서 빨간색이 된다.
  >
  > ★★ **무제한은 숫자가 아니라 술어다.** `dailyViews: 0` 이 무제한이고
  > `logic/adReward.js:AD_UNLIMITED` 가 그 뜻을 갖는다. 999 같은 큰 수로 흉내 내면
  > 화면이 "오늘 999회 남음"을 그리고, 하네스가 그것을 상한으로 착각하며,
  > 검사기는 "상한이 있다"로 통과시킨다 — **셋 다 거짓말이다.**
  > 무제한이면 남는 제동 장치는 **쿨다운 30초 하나뿐**이고, 테스트가 그것이 0 이
  > 되지 않도록 지킨다.
  >
  > ★★ **광고를 켜면 잠들어 있던 테스트가 깨어난다.** `adReward.test.js` 가
  > `AD_ENABLED` 로 `it.skip` 되어 있어 **한 번도 실행된 적이 없었고**, 켜는 순간
  > 하나가 실패했다(하루 1회에서는 쿨다운이 도달 불가능한 죽은 설정이었다).
  >
  > **`check:prod` 가 광고 배선을 지킨다 (A1–A4)** — 켜 놓고 단위 id 가 빈 값 ·
  > 앱 id(`~`)와 단위 id(`/`) 뒤바뀜 · `testMode` 배포 · 매니페스트에 테스트 앱 id
  > 잔존. 전부 **조용히 실패하는** 것들이라 기계가 잡는다.
  >
  > 배선: `data/ads.json`(수치) · `logic/adReward.js`(규칙 · 순수) ·
  > `store/slices/metaSlice.js:claimAdBonus` · `native/ads.js`(어댑터) ·
  > `screens/BattleResult.jsx`(**결과 화면 한 곳뿐**).
  > **실행 순서서: `docs/06-release/58-free-ads-release-playbook.md`** ·
  > 기술 상세 `56-admob-rewarded-integration.md`
- 라우트 변경마다 Phaser 파괴/재생성
- `asset/` 원본을 `public/` 에 복사
- 아이콘 `Separated Files/` (6,576 낱장) 아틀라스 패킹
