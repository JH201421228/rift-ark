# RIFT ARK — 작업 핸드아웃 (2026-08-05 ~ 08-06)

> 이 문서는 **이번 세션에서 무엇이 바뀌었고, 무엇이 왜 남았는지**의 요약이다.
> 규범은 여전히 `CLAUDE.md` 와 `docs/` 다 — 이 문서와 다르면 그쪽이 맞다.

**빌드:** `riftark-2026-08-05.apk` (30MB · 08-05 23:50)
**검증:** lint 0 · 테스트 **1,176**(64파일) · `data:validate` 경고 0 · `check` 전항 통과 · `check:prod` 통과
**밸런스:** `balance:check` **16/21** — 실패 다섯은 전부 **판정 대기**이지 회귀가 아니다 (§4)

---

## 1. 전투 — 규칙이 실제로 작동하기 시작했다

### 1.1 지휘관이 실제로 맞는다

설계 문서는 4개월 동안 "평타를 넣으려면 앞에 나가야 하고 **그러면 지휘관이 맞는다**"고
써 놓았는데, 지휘관을 때리는 코드는 **보스 슬램 하나뿐**이었다. 지휘관은 `lanes[].allies`
에 없어 `nearestTarget` 의 후보조차 아니었다 — 평타는 공짜였다.

> **규칙은 한 문장이다: 적은 지휘관을 맨 마지막에 본다.**
> 사거리 안에 동료가 하나라도 있으면 그를 때리고, **하나도 없을 때에만** 같은 지상
> 레인의 지휘관을 노린다. "가장 가까운 것"으로 두면 지휘관이 전선에 서는 순간 무리
> 전체가 몰려 **미끼가 아니라 즉사**가 된다.

- 원거리 적의 **탄도 지휘관에 맞는다** — 안 그러면 "조준은 하는데 통과하는" 죽은 규칙이 된다.
- 피해에는 **상한**이 있다 (`combat.enemyHitCommanderHpRatio` 0.08 · 최소 12방).
  지휘관 HP 600 고정 vs 적 ATK 10 → 342,133 (실측 34,000배) — 배율로는 절대 못 맞춘다.
- 상한·차감·기절 신호를 `logic/commanderHit.js:damageCommander` **하나**로 모았고
  **보스 슬램도 같은 함수를 쓴다.**
- 실측(자동 조작): 2~12판에 한 번 기절, 2-5 에서 평균 **600 중 546** 손실.

### 1.2 "지휘관이 서 있는가"는 술어 하나다

이 조건은 **두 항**(`hp > 0` · `t >= downUntil`)인데 저장소 **일곱 곳**이 각자 적고
있었고 **넷이 시간 항만** 보고 있었다. 그것들이 맞았던 이유는 `stepCommander` 가 앞에서
도는 **순서 덕분**이지 조건이 옳아서가 아니다 — 지휘관이 죽는 그 틱에는 `downUntil` 이
아직 0 이라 **죽은 지휘관이 이동했고 주문이 나갔다.**
→ `commanderHit.js:commanderUp()` 하나로 모았고, `downUntil` 직접 참조를 상태를 **쓰는**
세 파일(`lifecycle` · `state` · `commanderHit`)로 제한하는 테스트를 넣었다.

### 1.3 아군 비행 4종이 발사체를 쏜다

적 원거리 11종을 고친 날, **아군 검사는 한쪽만 돌고 있어서** 사거리 160–200 짜리
FLYER 가 즉발로 남아 있었다. 역할 단위로 `PROJECTILE_ROLES` 에 `FLYER` 를 넣었고
(`SUPPORT` 만 면제 — 힐은 `tryAttack` 을 지나지 않는다), `data:validate` 의
사거리↔역할 대조를 **양 진영 모두** 돌게 했다.
덤으로 `presenters.json:FLYER.attack` 이 근접 프로파일이라 **탄이 날아가는 중에 표적에서
타격이 먼저 터지고 있었다** — 발사체 프로파일로 교정했다.

### 1.4 나이트메어 난이도

월드마다 규칙 **정확히 하나**. 확률은 없고 동시 엔티티도 늘지 않는다.

| 월드 | 규칙 |
|---|---|
| 1 · 3 | **역병 장판** — 지상에서 죽은 적이 장판을 남긴다 (방어·결계 무시) |
| 2 · 4 | **결박 파열** |
| 5 | **고갈** — 처치 환급이 없고 소환 코스트 감쇠가 느려진다 |

판정은 `logic/nightmare.js:nightmareFor(world)` **하나**이고 화면·프리뷰·시뮬·검사기가
같이 부른다 — 화면이 `world === 5` 로 분기하지 않는다.
게이트 `npm run balance:nightmare` (BN1–BN8).

**규칙이 실제로 편성을 바꾼다** (300시드 · BN4 통과):

```
1-19 역병 장판   지배 전략 0.0%  →  지속회복 편성  33.3%
2-19 결박 파열   지배 전략 0.0%  →  지연 편성      91.7%
3-19 역병 장판   지배 전략 0.0%  →  지속회복 편성 100.0%
4-19 결박 파열   지배 전략 0.0%  →  화력 편성      58.3%
```

### 1.5 지휘관 성장 — 약한 게 아니라 **함께 자라지 않았다**

```
지휘관 레벨 성장   덧셈 0.04/레벨   →  만렙 30 에서 ×2.16 이 천장
동료  레벨 성장   곱셈 1.055/레벨  →  같은 캠페인에서 ×200 넘게
```

진행할수록 지휘관은 **동료 한 기의 1/100** 로 줄어들었다. 곱셈(`atkGrowth 1.105`)으로 바꿨다.

| 레벨 | 공격력 | DPS | (예전 DPS) |
|---|---|---|---|
| 1 | 18 | 20 | 20 ← 그대로 |
| 10 | 44 | 49 | 27 |
| 20 | 120 | 133 | 35 |
| 30 | 326 | 362 | 43 |
| 30 + 심판의 창 | 332 | **424** | 43 |

**배율은 손으로 고르지 않았다** — 동료가 방주 훈련장을 절반까지 키웠을 때의 레벨
상한에서 갖는 배율(×18)과 같게 맞췄다. 즉 **만렙 지휘관 = 그 시점의 잘 키운 동료 한 기.**
그 이상은 주지 않는다: 캐리가 되면 "오라를 어디에 둘 것인가"라는 실력 천장이 사라진다.
`commander.test.js` 가 **양쪽 경계**를 잡는다 (너무 낮아도, 캐리가 돼도 실패).

> ★ **레벨 1 은 정확히 0** 이라 하네스가 재는 곡선이 흔들리지 않는다 (50시드로 확인).
> **HP 는 곱셈으로 바꾸지 않았다** — 한 방이 최대 HP 비율로 상한이 걸려 있어
> HP 를 올려도 **버티는 타수가 같다.** 효과 없는 값을 키우면 골드만 빨아들인다.

---

## 2. 화면 — 읽을 수 있게

### 2.1 피아 식별 (사용자 제보: "그래픽 상으로는 구분이 잘 안돼")

| 기대했던 단서 | 왜 안 됐는가 |
|---|---|
| 스프라이트 그림 | **양 진영이 같은 시트**에서 온다 (몬스터 150종·NPC 49종을 나눠 쓴다) |
| 바라보는 방향 | 16×16 4프레임 그림은 **대칭인 것이 많다** |
| 체력바 색 | 아군 바는 **피해 뒤에만** 뜬다 → **만피 아군엔 단서가 0** |

→ **아군 = 채운 하늘색 타원 · 적 = 빈 붉은 링**을 발밑에 상시 깐다.
**색이 아니라 모양이 1차 단서**다 — 색약 모드에서도, 방주 피격으로 화면이 붉게 물드는
순간에도 갈린다. 유닛 **뒤**(depth 60)에 그려 픽셀아트를 덮지 않는다.
수치는 `presenters.json:sideMark`. 타원 분할은 12 로 낮췄다(기본 32 면 동시 200체에
프레임당 6,400 조각 — 사각형 체력바는 2 조각이다).

### 2.2 세 화면이 같은 2단 관습을 쓴다

`Meta.module.css` 의 `.split` / `.splitPane` / `.detailScroll` / `.detailFoot` 하나를
**동료 › 성장 · 동료 › 영입 · 편성 › 편성**이 공유한다. 실측 폭:

```
915×412   443.5 / 443.5 px      1280×720   620 / 620 px
```

바깥은 스크롤하지 않고 **각 칸이 자기 안에서만** 흐른다. 상세의 머리·발은 고정 —
세로 412px 에서 실행 버튼(레벨업 · 영입)이 스크롤 밖으로 밀리면 그 탭에서 할 수 있는
유일한 행동이 사라진다.

- **편성**: 슬롯 6칸은 스크롤하지 않는다. 고르는 쪽이 흐르는 동안 **놓는 쪽이 보여야** 한다.
  `.slots` 트랙은 `repeat(6, minmax(0, 1fr))` — `1fr` 의 최소 폭은 `auto` 라
  **긴 이름 하나가 6칸을 밀어낸다.**
- **영입**: 카드에서 `disabled` 를 걷어냈다. 잠긴 것도 보유한 것도 **눌러서 읽을 수 있어야**
  한다. 지출은 [영입한다] 하나로 모았고 그 콜백 첫 줄이 `canRecruit` 를 다시 본다.
  못 사는 이유를 문장으로 말한다 (`골드가 2,490 부족합니다 (보유 10)`).
- **지휘관은 편성 탭에 없다** — 위쪽 [지휘관] 탭이 유일한 자리다.

### 2.3 그 밖

- **지휘관 HP 게이지** (전투 HUD) — 방주 옆 금색 바, 기절 중에는 "기절".
  재출격 카운트다운은 넣지 않았다 (`CommanderPresenter` 가 지휘관 위에 이미 그린다).
- **출격 초기 포커스** — 선택과 월드가 **같은 출처**(`nextStageId`)에서 나온다.
  그 카드로 스크롤도 맞춘다 (사용자가 직접 고를 때는 튀지 않게 제외).
- **가이드 23주제** — `battle.commander`("지휘관 — 앞에 나가면 맞습니다") 추가.
- 나이트메어 규칙 배지 (출격 난이도 카드 · 스테이지 프리뷰).

---

## 3. 이번에 배운 것 — 기계가 잡게 한 것들

이 저장소의 단일 실패 유형은 **"선언했는데 아무도 안 읽는 것"** 과
**"같은 사실이 두 곳에"** 다. 이번에도 같은 모양으로 나왔고, 전부 검사기를 붙였다.

| 사고 | 지금 막는 것 |
|---|---|
| `hidden` 속성이 `display:flex` 에 진다 → 두 탭이 겹쳐 보였다 | `loadoutPanes.test.js` (조건부 렌더 강제) |
| `startBattle` 이 부분집합만 받아 **지휘관 게이지가 통째로 사라졌다** | `runSlice.test.js` (씬 payload ↔ cfg 참조 대조) |
| 새 필드를 얕은 비교에 안 넣으면 **그 값만 바뀐 동기화가 버려진다** | `runSlice.test.js` (payload ↔ 비교식 대조) |
| `disabled` 는 `pointerdown` 을 못 막는다 → 보유 카드에 구매 모달이 떴다 | `recruitCard.test.js` (지출 경로 가드) |
| 가이드 본문의 **홑별표가 화면에 그대로 찍혔다** | `data:validate` (강조는 `**…**` 뿐) |
| 지휘관 술어가 일곱 곳에 복제 | `commanderTarget.test.js` (`downUntil` 직접 참조 제한) |
| 지휘관 성장이 "오르기는 하지만" 무의미 | `commander.test.js` (배율의 **크기**를 데이터에서 파생한 기준으로) |
| 새 데이터 필드에 소비처 없음 | `data:validate` `FIELD_CONSUMERS` — 실제로 내 `sideMark` 를 먼저 잡았다 |

> **모든 검사기는 일부러 깨뜨려 실패를 확인한 뒤에 넣었다.** 그 과정에서 내가 쓴 검사기
> **셋이 아무것도 지키지 않는 채 통과**하고 있었다 — 블록 범위를 잘못 잘랐고(두 곳),
> 기준선이 너무 물렀다(한 곳). 깨뜨려 보지 않았으면 그대로 나갔다.

---

## 4. 남은 것 — **사용자 판정 대기** (회귀가 아니다)

`balance:check` **16/21**. 실패 다섯은 **두 가지 원인**으로 수렴한다.

| 게이트 | 실측 | 실체 |
|---|---|---|
| **B3** (하드) | 1-9 승률 **84.3%** / 목표 30–45% | "설계된 첫 패배"가 너무 쉽다 |
| **BN3** (하드) | 5-20 나이트메어 **0%** | **규칙을 전부 꺼도 0%** |
| B10 (소프트) | ★2 73.8% / 목표 45–60% | 위와 같은 곡선 |
| BN5 (소프트) | 월드 1–3 만렙이면 규칙과 무관하게 100% | 파워 곡선이 콘텐츠보다 가파르다 |
| BN8 (소프트) | 5-19 하드 ★2 **0%** | → **월드 5 나이트메어가 영영 안 열린다** |

**나이트메어가 만든 문제가 아니라 배율 × 성장 곡선이 드러난 것이다.**
배율을 1.6 까지 낮춰도 5-19 는 0% 이고 월드 1–4 만 더 시시해진다 — 한쪽을 고치면 다른
쪽이 나빠지므로 **어느 쪽을 우선할지가 설계 판단**이다.

### 직접 해 보고 정해 주셔야 하는 것

1. **월드 5 후반** (5-12 · 5-19 · 5-20) — 지금은 하드에서도 38%, 나이트메어는 0%.
   배율을 낮출 것인가, 그 구간을 "완주하는 곳"이 아니라 "도전하는 곳"으로 둘 것인가.
2. **1-9** — 설계상 첫 패배인데 84% 로 이긴다. 더 어렵게 만들 것인가.
3. **지휘관 세기** — 만렙 424 DPS 로 맞췄다. 실제로 플레이하는 레벨대(10~20)는 49~133 DPS.
   부족하면 `commander.json:level.atkGrowth` **하나**만 올리면 되고,
   어디부터가 "캐리"인지는 테스트가 경계를 잡아 준다.

### 그 밖 남은 티켓

- **P9-01~03 실기기 검증** — 콜드 스타트 · 세이브 유지 · 씬 전환 스트레스
- 1280×720 에서 편성·성장 탭의 좌측 칸 아래 빈 공간 (폰 비율 기준으로 맞춘 결과)

---

## 5. 다음 사람이 알아야 할 것

- **UI 를 만들었으면 화면을 실제로 띄워 본다.** 이번 세션에서 lint · 테스트 1,100+ ·
  검사기 셋을 **전부 통과한 채로** 화면에서만 드러난 결함이 셋 있었다.
  합성 이벤트로는 안 보이는 것이 있다 — CDP `Input.dispatchMouseEvent` 로 진짜 입력을 보내야 한다.
- **검사기는 일부러 깨뜨려 본다.** 통과만 보고 넣으면 아무것도 안 지키는 검사기가 남는다.
- **밸런스 게이트 다섯 실패는 정상 상태다.** 여섯 번째가 생겼을 때만 자기 변경을 의심한다.
- 하네스는 **지휘관 레벨 1** 로 잰다 — 지휘관 성장 변경은 게이트를 움직이지 못한다.
